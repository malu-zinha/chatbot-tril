# Nixpacks Configuration for Railway
# Forces Node.js detection instead of Deno

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = []

[start]
cmd = "npx ts-node --esm src/server-twilio.ts"
