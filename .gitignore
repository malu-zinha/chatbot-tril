# Environment variables
# Cobre .env, .env.local, .env.production, .env.development, .env*.local —
# antes só `.env` estava ignorado, e os demais entrariam no commit sem aviso.
.env*
!.env.example

# macOS
.DS_Store

# Windows NTFS alternate data stream artifacts
*:Zone.Identifier

# VSCode
.vscode/

# Node modules
node_modules/

# Build
dist/
*.log

# WhatsApp session
.wwebjs_auth/
.wwebjs_cache/

# Google Credentials (IMPORTANTE: nunca commitar!)
credentials.json
*-credentials.json

# Temporary audio files
temp/
*.ogg
*.mp3
*.wav

auditoria-conclusoes-*.csv
correcao-conclusoes-*.sql
